#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main()
{
    // vector<int> a{1, 2, 5, 2, 1};
    // int n = a.size();
    // int res = 0;

    // for (int i = 0; i < n; i++)
    // {
    //     res = res ^ a[i];
    // }

    // cout << res;

    cout << (5 ^ 2);
    return 0;
}