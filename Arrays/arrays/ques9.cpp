#include "bits/stdc++.h"
using namespace std;

int main()
{
    // input array-----
    int m1, n1, m2, n2;
    int arr[m1][n1];
    int arr[m2][n2];

    for (int i = 0; i < m1; i++)
    {
        for (int i = 0; i < n1; i++)
        {
            cin >> arr[m1][n1];
        }
    }

    for (int i = 0; i < m2; i++)
    {
        for (int j = 0; j < n2; j++)
        {
            cin >> arr[m2][n2];
        }
    }

    // --------------------------------

    return 0;
}